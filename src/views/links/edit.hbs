<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/links/edit/{{links.idservicioespera}}" method="POST">
                        <div class="form-group">
                            <input type="date" value="{{links.fecha}}" name="fecha" class="form-control" autofocus>
                        </div>
                        <div class="form-group">
                            <input type="text" value="{{links.direccion}}" name="direccion" class="form-control" placeholder="Direccion">
                        </div>
                        <div class="form-group">
                            <input type="text" value="{{links.barrio}}" name="barrio" class="form-control" placeholder="Barrio">
                        </div>
                       
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

    </div>
</div>